import { useState } from 'react';
import { Icon } from '@/components/ui/Icons';

const faqs = [
  {
    q: 'Какие данные получает TradeumDiary?',
    a: 'Только данные, необходимые для импорта завершённых сделок: инструмент, направление, исполнения, комиссии и итоговый P&L. Доступ к выводу средств не запрашивается.',
  },
  {
    q: 'Есть ли доступ к моим средствам?',
    a: 'Нет. Подключение источника используется только для чтения торговой истории. Права на торговлю и вывод средств не нужны.',
  },
  {
    q: 'Что доступно бесплатно?',
    a: 'На Free доступны подключение поддерживаемого источника, импорт завершённых сделок, базовый журнал и основные показатели. PRO за 499 ₽ в месяц добавляет углублённую аналитику.',
  },
  {
    q: 'Какие источники уже поддерживаются?',
    a: 'Актуальный список и статус каждого подключения всегда показаны в разделе «Источники». Недоступные интеграции отмечены как «Скоро» и не выдаются за работающие.',
  },
  {
    q: 'Как рассчитываются сделки?',
    a: 'Частичные исполнения объединяются в завершённые позиции. Метрики строятся по итоговому результату с учётом комиссий, а не по отдельным ордерам.',
  },
  {
    q: 'Можно ли отменить PRO?',
    a: 'Да. Управление тарифом находится в настройках. Базовый дневник остаётся доступным на Free.',
  },
];

export function FAQSection() {
  const [open, setOpen] = useState<number | null>(0);
  return (
    <section id="faq" className="landing-v7-faq" aria-labelledby="faq-title">
      <header>
        <span>FAQ</span>
        <h2 id="faq-title">Коротко о данных, доступе и тарифах.</h2>
      </header>
      <div>
        {faqs.map((faq, index) => (
          <article key={faq.q} className={open === index ? 'is-open' : ''}>
            <button
              type="button"
              onClick={() => setOpen(open === index ? null : index)}
              aria-expanded={open === index}
            >
              <span>{faq.q}</span>
              <Icon name="chevron-down" size={18} />
            </button>
            {open === index ? <p>{faq.a}</p> : null}
          </article>
        ))}
      </div>
    </section>
  );
}
