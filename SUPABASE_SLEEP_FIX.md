# 🚨 Решение проблемы "ERR_CONNECTION_RESET" с Supabase

## Проблема

База Supabase на **бесплатном тарифе** "засыпает" после 1 часа бездействия и:

- ❌ Первые запросы занимают 30-60 секунд
- ❌ Часто возвращает `ERR_CONNECTION_RESET`
- ❌ Иногда `ERR_HTTP2_PING_FAILED`

---

## ✅ Решение 1: "Разбудить" базу вручную (быстро)

### Шаг 1: Откройте Supabase Dashboard

🔗 https://app.supabase.com/project/TradeumD

### Шаг 2: Перейдите в SQL Editor

Нажмите **SQL Editor** в левом меню

### Шаг 3: Выполните простой запрос

```sql
SELECT NOW();
```

### Шаг 4: Подождите 10-20 секунд

База "проснётся"

### Шаг 5: Обновите приложение

Нажмите `F5` в браузере

---

## ✅ Решение 2: Использовать wake-up endpoint (автоматически)

### Шаг 1: Разверните функцию

1. Откройте: https://app.supabase.com/project/TradeumD/functions
2. Нажмите **"New function"**
3. Slug: `wake-up-db`
4. Скопируйте содержимое: `supabase/functions/wake-up-db/index.ts`
5. Нажмите **Deploy**

### Шаг 2: Вызывайте функцию раз в 30 минут

Откройте консоль браузера (F12) и выполните:

```javascript
// Пробудите базу
fetch('https://zfgeofskmgycojbzrznk.supabase.co/functions/v1/wake-up-db', {
  method: 'POST',
})
  .then((r) => r.json())
  .then(console.log);
```

Или добавьте в приложение:

```javascript
// Пробуждаем базу каждые 30 минут
setInterval(
  async () => {
    await fetch('/functions/v1/wake-up-db', { method: 'POST' });
  },
  30 * 60 * 1000
);
```

---

## ✅ Решение 3: Увеличить timeout (уже сделано)

Я увеличил timeout для запросов:

- **120 секунд** (вместо 30)
- **2 повторения** при сбоях

Это поможет при "медленном пробуждении" базы.

---

## ✅ Решение 4: Увеличить тариф Supabase (рекомендуется)

**Pro тариф ($25/месяц):**

- ❌ База НЕ засыпает
- ✅ Постоянная доступность
- ✅ Быстрые запросы

**Бесплатный тариф:**

- ⚠️ База засыпает после 1 часа
- ⚠️ Первые запросы 30-60 секунд
- ⚠️ Частые `ERR_CONNECTION_RESET`

---

## 📋 Пошаговая инструкция (прямо сейчас)

1. **Откройте Supabase Dashboard**
   🔗 https://app.supabase.com/project/TradeumD/sql

2. **Выполните запрос:**

   ```sql
   SELECT NOW();
   ```

3. **Подождите 10-20 секунд** пока выполнится

4. **Вернитесь к приложению** и обновите страницу (`F5`)

5. **Попробуйте добавить Bybit:**
   - Нажмите "Подключить"
   - Выберите "Биржа (CEX)"
   - Нажмите **Bybit** (синяя иконка)
   - Введите API ключи
   - Нажмите "Проверить подключение"

---

## 🐛 Если всё ещё не работает

### Проверьте логи Supabase:

1. Dashboard → **Logs** → **Postgres Logs**
2. Посмотрите последние ошибки

### Проверьте статус региона:

🔗 https://status.supabase.com

Если регион имеет проблемы → ждите восстановления.

---

## 📞 Если ничего не помогает

**Варианты:**

1. **Увеличить тариф** до Pro ($25/месяц)
2. **Перенести на другой хостинг** (AWS, GCP, Azure)
3. **Использовать локальную базу** для разработки

---

**Попробуйте выполнить "Шаг 1-5" выше прямо сейчас!**
